= Ukázkové instance a demonstrace

Tato kapitola představuje vybrané ukázkové instance
pro všechny implementované redukce.
Uživatelé mohou samozřejmě zadávat vlastní instance,
ale systém jim zároveň umožňuje vybírat z předdefinovaných ukázkových příkladů.
Díky tomu se mimo jiné mohou naučit, v jakém formátu editor očekává zadání instancí.
Na konkrétních příkladech je demonstrováno,
jak aplikace vizualizuje proces redukce krok za krokem.

Zvolené instance pokrývají:

- různé velikosti vstupních dat,
- případy s kladnou i zápornou odpovědí,
- různorodé struktury vstupních instancí (formule s různými kombinacemi literálů, grafy s různou hustotou hran).

Pro každou redukci jsou v kapitole uvedeny dva vybrané příklady:
jedna instance s kladnou odpovědí a jedna se zápornou odpovědí.
U každé instance je vysvětleno, proč byla zvolena,
a jsou zde uvedeny i snímky obrazovky aplikace.

V aplikaci jsou pro každou redukci k dispozici minimálně 4 ukázkové instance (2 kladné, 2 záporné),
přičemž v této kapitole jsou detailněji rozebrány pouze dvě z nich.
Celkem aplikace nabízí 20 ukázkových instancí,
což poskytuje uživatelům dostatek materiálu pro experimentaci.

#include "./6.3sat-hcycle.typ"
#include "./6.3sat-ssp.typ"
#include "./6.3sat-3cg.typ"
#include "./6.hcycle-hcircuit.typ"
// #include "./6.hcircuit-tsp.typ"
